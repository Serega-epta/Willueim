#category = short_term medium_term or long_term
# ROOT is faction leader, FROM is faction member who opens UI and views goals, completed/cancel/complete_effect do not have FROM scopes


faction_goal_resource_control = {
	name = resource_control_goal_name
	description = resource_control_goal_desc
	category = long_term
	group = resources_to_market_icon
	visible = {

	}
	available = {
		
	}
	completed = {
		has_resources_in_collection = {
			collection = {
				input = game:scope
				operators =  { faction_members }
				name = FACTION
			}
			resource = oil
			amount > 499
			extracted = yes # (optional, default: no) checks extracted amount instead of country balance
		}
		has_resources_in_collection = {
			collection = {
					input = game:scope
					operators =  { faction_members }
					name = FACTION
			}
			resource = coal
			amount > 499
			extracted = yes # (optional, default: no) checks extracted amount instead of country balance
		}
		has_resources_in_collection = {
			collection = {
					input = game:scope
					operators =  { faction_members }
					name = FACTION
			}
			resource = rubber
			amount > 199
			extracted = yes # (optional, default: no) checks extracted amount instead of country balance
		}
		has_resources_in_collection = {
			collection = {
					input = game:scope
					operators =  { faction_members }
					name = FACTION
			}
			resource = aluminium
			amount > 199
			extracted = yes # (optional, default: no) checks extracted amount instead of country balance
		}
		has_resources_in_collection = {
			collection = {
					input = game:scope
					operators =  { faction_members }
					name = FACTION
			}
			resource = chromium
			amount > 199
			extracted = yes # (optional, default: no) checks extracted amount instead of country balance
		}
		has_resources_in_collection = {
			collection = {
					input = game:scope
					operators =  { faction_members }
					name = FACTION
			}
			resource = tungsten
			amount > 199
			extracted = yes # (optional, default: no) checks extracted amount instead of country balance
		}
	

	}
	complete_effect = {
		add_faction_power_projection = 500
		custom_effect_tooltip = generic_skip_one_line_tt
		custom_effect_tooltip = THE_FACTION_LEADER
		add_faction_initiative = 1
		custom_effect_tooltip = generic_skip_one_line_tt
		every_faction_member = { 
			tooltip = INFLUENCE_TOP_THREE
			visible_when_empty = yes
			limit = {
				faction_influence_rank < 4
			}
			add_political_power = 100
		}
	}
	ai_will_do = {
		base = 1
	}
}
faction_goal_rebuild_motherland = {
	name = rebuild_motherland_goal_name
	description = rebuild_motherland_goal_desc
	category = long_term
	group = resources_to_market_icon
	visible = {
		OR = {
			has_government = statesmen
			has_government =  militarists
			has_government = humanists
		}
	}
	available = {
		
	}
	completed = {
		collection_size = {
			input = {
				input = collection:mother_russia
				operators = {
					limit = {
						NOT = {
							is_controlled_by_ROOT_or_ally = yes
						}
					}
				}
				name = COLLECTION_NON_MOTHER_LAND
			}
			value < 1
		}
	}
	complete_effect = {
		every_faction_member = {
			add_stability = 0.05
			add_war_support = 0.15
		}
	}
}
faction_goal_destroy_heresy = {
	name = destroy_heresy_goal_name
	description = destroy_heresy_goal_desc
	category = long_term
	group = resources_to_market_icon
	visible = {
		NOT = {
			has_government = chthonism
			has_government = sacraments
		}
	}
	available = {
		
	}
	completed = {
		all_country = {
			NOT = {
				has_government = chthonism
				has_government = sacraments
			}
		}
	}
	complete_effect = {
		every_faction_member = {
			add_stability = 0.1
		}
	}
}
faction_goal_restore_nuclear_shield = {
	name = restore_nuclear_shield_goal_name
	description = restore_nuclear_shield_goal_desc
	category = long_term
	group = resources_to_market_icon
	visible = {
		
	}
	available = {
		OR = {
			collection_size = {
				input = {
					input = collection:all_existing_states
					operators = {
						limit = {
							is_controlled_by_ROOT_or_ally = yes 
							nuclear_reactor > 0
						}
					}
					name = COLLECTION_FACTION_NUCLEAR_REACTOR
				}
				value < 10
			}
			collection_size = {
				input = {
					input = collection:all_existing_states
					operators = {
						limit = {
							is_controlled_by_ROOT_or_ally = yes 
							nuclear_reactor_heavy_water > 0
						}
					}
					name = COLLECTION_FACTION_NUCLEAR_REACTOR_HEAVY_WATER
				}
				value < 10
			}
		}
	}
	completed = {
		collection_size = {
			input = {
				input = collection:all_existing_states
				operators = {
					limit = {
						#TODO: spam in error.log, but works correctly
						#is_controlled_by_ROOT_or_ally = yes 
						nuclear_reactor > 0
					}
				}
				name = COLLECTION_FACTION_NUCLEAR_REACTOR
			}
			value > 9
		}
		collection_size = {
			input = {
				input = collection:all_existing_states
				operators = {
					limit = {
						#TODO: spam in error.log, but works correctly
						#is_controlled_by_ROOT_or_ally = yes 
						nuclear_reactor_heavy_water > 0
					}
				}
				name = COLLECTION_FACTION_NUCLEAR_REACTOR_HEAVY_WATER
			}
			value > 9
		}
	}
	complete_effect = {
		every_faction_member = {
			add_popularity = {
				ideology = THIS
				popularity = 0.1
			}
			add_war_support = 0.05
			add_stability = 0.05
		}
	}
}