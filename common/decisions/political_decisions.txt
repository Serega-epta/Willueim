political_actions = {
	ROH_rez_nya = {
	
		activation = {
			has_active_mission = ROH_rez_nya
		}
		available = {
			hidden_trigger = { always = no }
		}
		
		icon = mission_icon
		
		is_good = yes
		days_mission_timeout = 14
		selectable_mission = no
		timeout_effect = {
			if = {
				limit = {
					has_war = no
				}
				random_neighbor_country = {
					declare_war_on = {
						target = ROOT
						type = annex_everything
					}
				}
			}
		}
	}
}