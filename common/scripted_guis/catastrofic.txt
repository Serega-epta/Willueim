scripted_gui = { # Тут мы описываем все скрипты
   catastrof = { # Для каждого независящего контейнера должны быть функция с уникальным названием, в ней мы будем прописывать скрипт

       context_type = player_context
		 window_name = "catastrof"
		parent_window_token = top_tab
       visible = {

       }
 
        effects = {
            
        }
        triggers = {
            
        }
   }
}