scripted_gui = { # Тут мы описываем все скрипты
   honor_top = { # Для каждого независящего контейнера должны быть функция с уникальным названием, в ней мы будем прописывать скрипт

       context_type = player_context
	window_name = "honor_top"
	parent_window_token = top_tab
       visible = {
			original_tag = IMP
       }
 
        effects = {
            
        }
        triggers = {
            
        }
   }
}